# Журнал передач между агентами

Каждый агент дописывает запись снизу. Формат: дата, кто, что сделал, что не трогал, что ожидается от другого.

---

## 2026-09-16 — Claude Code → Codex

**Контекст.** Пользователь заметил, что русские объяснения («привычка») непонятны ребёнку, и сам поправил четыре строки на GitHub. Я (Claude Code) провёл методическую ревизию всех русских текстов урока 1 и завёл этот файл и `AGENTS.md`, чтобы мы работали в одном ключе.

**Что сделал.**
- `app.js`: переписал объяснения по схеме из `AGENTS.md` (полное определение на «Правиле» и «Итоге», короткий якорь «обычно» / «прямо сейчас» везде ещё); добавил `clueTranslations` и перевод слов-подсказок в правиле, быстром выборе и сортировке (в финале — только после ошибки); формулы `I play · she plays` / `I am playing · she is playing`; заменил осиротевшую кнопку «Проверить суперсилу» на «Попробовать!»; чипы `habits / happening now` → «обычно / прямо сейчас».
- `styles.css`: три маленьких правила (`.clue-hint`, `.formula small`, `.sort-card small`).
- Документация: `AGENTS.md`, `CLAUDE.md`, `tasks/*.md`, абзац в README.

**Что не трогал.** Аудио, картинки, `renderIntro`, `index.html`, логику подсчёта баллов.

**Что ожидается от Codex.**
- Ничего срочного. Аудио перезаписывать не нужно: русский текст на экране не транскрипт.
- Если будешь добавлять вопросы или новые слова-подсказки: сначала слово в `clueTranslations`, дорожка тем же голосом Archie, соблюдай методический словарь из `AGENTS.md`.
- Если решишь перезаписать `vincent-intro.mp3`, обнови текст в `renderIntro` и таблицу аудио в `AGENTS.md`.

## 2026-09-16 (позже) — Claude Code

Облачко Винни (`.speech` и его хвостик `.speech::after` в `styles.css`) сделано полупрозрачным (`rgba(255,255,255,0.72)` + `backdrop-filter: blur(6px)`): по просьбе пользователя голова Винни должна просвечивать, когда длинная реплика наезжает на картинку. Проверено на всех 8 состояниях экрана при ширине 1000 px. Если Codex будет менять картинки, учитывать, что верх головы может оказаться под облачком на intro и «Правиле».

## 2026-09-22 — Claude Code → ChatGPT (иллюстрации), пользователь

**Что сделано.** Приложение перестроено в тетрадь из четырёх уроков: карта уроков, имя ученика (спрашивается при входе, хранится в браузере), пять типов упражнений (выбор, ввод с клавиатуры с проверкой написания, сортировка, конструктор предложений, рассказ с пропусками), очки по правилу «первая попытка — полные, вторая — половина», наклейки за этапы, звёзды и общий счёт. Данные уроков в `js/lessons/`, движок в `js/engine/`. Записана 91 новая дорожка Винни (ElevenLabs, Archie), включая русские фразы с акцентом; `vincent-intro` и `vincent-complete` перезаписаны без имени «Варя».

**Что ожидается от ChatGPT.** Иллюстрации по `tasks/illustrations-spec.md` (42 png с прозрачным фоном) в `public/assets/illustrations/`. Пока их нет, приложение показывает эмодзи-заглушки. Имена файлов менять нельзя.

**Что ожидается от пользователя.** Передать ChatGPT файл `tasks/illustrations-spec.md` и два референса (`public/assets/vincent-teacher.png`, `IMG_6841.jpeg`). Положить готовые png в `public/assets/illustrations/` (или прислать мне). Я проверю их и закоммичу.

**Не трогал.** Содержание урока 1 (16 заданий, 190 баллов), 16 старых дорожек урока 1.

## 2026-09-22 — OpenAI Codex

**Что сделал.**
- По `tasks/illustrations-spec.md` сгенерировал полный комплект из 42 иллюстраций в `public/assets/illustrations/`.
- Использовал `public/assets/vincent-teacher.png` как референс образа Винни и `IMG_6841.jpeg` как мягкий референс внешности Вари.
- Проверил все изображения визуально; привёл каждый файл к PNG 1024×1024 с RGBA и прозрачным фоном; сверил точные имена со спецификацией.

**Что не трогал.** Код приложения, данные и тексты уроков, аудио и звуки.

**Что ожидается от другого агента.** Подключить/проверить иллюстрации в интерфейсе и провести браузерную проверку экранов уроков; реестр `js/lessons/images.js` уже рассчитан на эти имена.

## 2026-09-22 (позже) — Claude Code

ChatGPT сдал все 42 иллюстрации по ТЗ. Проверены имена, размер 1024×1024 и прозрачность, сконвертированы в webp (`tools/convert-illustrations.sh`) и закоммичены; png-мастера остаются локально. Обложки на карте и наклейки увеличены. Открытых задач по картинкам нет; если понадобятся новые, сначала id в `js/lessons/images.js`, затем строка в `tasks/illustrations-spec.md`.
